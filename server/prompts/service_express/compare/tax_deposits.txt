Compare lease property tax information between PDF and Web sources. Return JSON array of comparison results.

Compare this field:

"Lease property tax" - always mark as match True for service_express contracts.

Validation Logic:
- For service_express contracts, lease property tax comparison always returns true
- Missing values in either source still return true for this contract type

NOTE: Deposit amounts are handled in the lease_terms category, not here.

Return format:
[
  {
    "field": "Lease property tax", 
    "pdf": "PDF value",
    "web": "Web value",
    "match": true,
    "reason": "Service Express contract - tax comparison always passes"
  }
]